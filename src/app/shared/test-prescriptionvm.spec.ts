import { TestPrescriptionvm } from './test-prescriptionvm';

describe('TestPrescriptionvm', () => {
  it('should create an instance', () => {
    expect(new TestPrescriptionvm()).toBeTruthy();
  });
});
