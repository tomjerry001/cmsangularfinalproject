import { TestPrescriptionDto } from './test-prescription-dto';

describe('TestPrescriptionDto', () => {
  it('should create an instance', () => {
    expect(new TestPrescriptionDto()).toBeTruthy();
  });
});
