import { MedicinePrescriptionvm } from './medicine-prescriptionvm';

describe('MedicinePrescriptionvm', () => {
  it('should create an instance', () => {
    expect(new MedicinePrescriptionvm()).toBeTruthy();
  });
});
